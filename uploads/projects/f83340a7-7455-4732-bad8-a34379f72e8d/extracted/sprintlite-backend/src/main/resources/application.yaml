spring:
  application:
    name: sprintlite-backend
    
  # Database Configuration
  datasource:
    url: jdbc:postgresql://ep-young-scene-aon34361-pooler.c-2.ap-southeast-1.aws.neon.tech:5432/neondb?sslmode=require
    username: neondb_owner
    password: npg_j0VBwFoGM9hp
    driver-class-name: org.postgresql.Driver
    hikari:
      maximum-pool-size: 10
      minimum-idle: 5
      connection-timeout: 30000
  
  # JPA Configuration
  jpa:
    hibernate:
      ddl-auto: none
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
        format_sql: true
        jdbc:
          batch_size: 20
  
  # Redis Configuration
  data:
    redis:
      host: localhost
      port: 6379
  
  # File Upload Configuration
  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 50MB
  
  # Email Configuration - DIRECT VALUES (No .env file)
  mail:
    host: smtp.gmail.com
    port: 587
    username: chevurukethankumar99@gmail.com
    password: oymskgmopolcuwhy
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true
            required: true
          connectiontimeout: 5000
          timeout: 5000
          writetimeout: 5000
    default-encoding: UTF-8

server:
  port: 8080
  error:
    include-message: always
    include-binding-errors: always
  address: 0.0.0.0
  tomcat:
    connection-timeout: 30000
    max-connections: 10000
    threads:
      max: 200
      min-spare: 10

# JWT Configuration
jwt:
  secret: 404E635266556A586E3272357538782F413F4428472B4B6250645367566B5970
  expiration: 86400000
  refresh-expiration: 604800000
  issuer: SprintLite

# CORS Configuration
# app:
#   cors:
#     allowed-origins: 
#       - http://localhost:3000
#       - http://localhost:5173

#       - http://localhost:5174
#       - http://127.0.0.1:3000
#       - http://127.0.0.1:5173
#     allowed-methods: GET,POST,PUT,DELETE,OPTIONS,PATCH
#     allowed-headers: "*"
#     allow-credentials: true
#     max-age: 3600

# Logging
logging:
  level:
    com.sprintlite: DEBUG
    org.springframework.web: INFO
    org.springframework.security: DEBUG
    org.springframework.web.cors: DEBUG
    org.springframework.mail: DEBUG
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} - %msg%n"

# OpenAPI/Swagger
springdoc:
  api-docs:
    path: /api-docs
    enabled: true
  swagger-ui:
    path: /swagger-ui.html
    enabled: true
    operationsSorter: method
    tagsSorter: alpha
    tryItOutEnabled: true
    filter: true
    displayRequestDuration: true
  show-actuator: true
  default-consumes-media-type: application/json
  default-produces-media-type: application/json

# Actuator
management:
  endpoints:
    web:
      exposure:
        include: health,info,metrics
      base-path: /actuator
  endpoint:
    health:
      show-details: always